<li>
  <%= role.name %>
  [<% if @user.has_role?(role.name) %>
    <%= link_to 'remove role',
                user_role_path(:id => role.id, :user_id => @user.id),
                :method => :delete %>
  <% else %>
    <%= link_to 'assign role',
                user_role_path(:id => role.id, :user_id => @user.id),
                :method => :put %>
  <% end %>] 备注:权限赋值有点问题。<br />
</li>
 
